# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8080/api/v1/auth/google/callback

# Test Google OAuth Configuration (for testing with different scopes)
GOOGLE_OAUTH_TEST_CLIENT_ID=your-test-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_TEST_CLIENT_SECRET=your-test-google-client-secret
GOOGLE_OAUTH_TEST_REDIRECT_URI=http://localhost:8080/api/v1/auth/google/test/callback

# Gemini API Key (for flashcard generation)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# Firestore Configuration
# Project ID: Get from Firebase Console (https://console.firebase.google.com/)
FIRESTORE_PROJECT_ID=your-firestore-project-id

# Service Account Key: Path to your Firebase service account JSON file
# Get from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Download the JSON file and set the path here (e.g., /path/to/serviceAccountKey.json)
# For production (Cloud Run/GCE), leave empty to use Application Default Credentials
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json
